<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.paulohdsousa.vocemeconhece">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RankingActivity" android:label="Ranking"/>
        <activity android:name=".CadastrarPerguntaActivity" android:label="Cadastrar Perguntas" />
        <activity android:name=".JogarActivity" android:label="Jogar" />
        <activity android:name=".SucessoActivity" android:label="Sucesso" />
        <activity android:name=".PerguntasActivity" android:label="Perguntas">

        </activity>
    </application>

</manifest>